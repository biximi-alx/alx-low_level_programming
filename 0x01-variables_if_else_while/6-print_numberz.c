#include <stdio.h>

/**
 * main - a program that prints all single digit numbers 
 * return: return always 0
 */
int main(void)
{
int ch;
for (n = 0; n < 10; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}
