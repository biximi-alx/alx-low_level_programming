#include <stdio.h>

/**
 * main - a program that prints the alphabet in lowercase, and then in uppercase
 *
 *return: success always 0
 */
int main(void)
{
  int n;
  for(n =0; n<10;n++)
  {
    printf("%d\n", n);
  }
 
  return (0);
}
