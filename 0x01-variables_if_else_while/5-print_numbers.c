#include <stdio.h>

/**
 * main - a program that prints the alphabet in lowercase, and then in uppercase
 *
 *return; always return 0
 */
int main(void)
{
	int a =0;
	for(; a < 10; n++)
	printf("%d\n", a);
	putchar('\n');
	return (0);
}
