#include <stdio.h>

/**
 * main - program that prints a given line of text
 * Return: Always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzl");
	return (0);
}
