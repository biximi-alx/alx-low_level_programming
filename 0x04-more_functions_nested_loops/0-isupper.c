#include "main.h"

/**
 *_isupper - cheches if a letter is upper case
 *@m:the number to be checked
 
 * Return: give 1 or 0
 */
int _isupper(int m)
{
	if (m >= 65 && m <= 90)
	{
		return (1);
	}
	return (0);
}
