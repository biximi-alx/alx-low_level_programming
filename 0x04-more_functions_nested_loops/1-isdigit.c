#include "main.h"

/**
* _isdigit - checkes if the no. is b/n 0 to 9
* @m: input no
* Return: 1 if is a number (0 to 9),0 in other case.
*/
int _isdigit(int m)
{
  if (m >= 48 && m <= 57)
  {
  return (1);
  }
  else
  {
  return (0);
  }
  -putchar('\n');
}  
